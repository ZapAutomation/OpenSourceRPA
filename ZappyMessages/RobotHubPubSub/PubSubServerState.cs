namespace ZappyMessages.RobotHubPubSub
{
    /// <summary>
    /// Enum specifying client state
    /// </summary>
    public enum PubSubServerState
    {
        Working,
        Busy,
        Idle,
        Error
    }
}